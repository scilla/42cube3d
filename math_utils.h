#ifndef MATH_UTILS_H
# define MATH_UTILS_H
# include "main.h"

t_point	rotate_point(double angle, t_point p);
double	fix_pi(double rot);
char	*ftoa(float f);
int		div_color(int cl, double mlt);

#endif